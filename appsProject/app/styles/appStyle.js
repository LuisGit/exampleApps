import { StyleSheet } from 'react-native';

export const appStyle = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export const theme = {
  Button: {
    titleStyle: {
      color: 'red',
    },
  },
};
